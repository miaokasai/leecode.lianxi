class Solution {
    public boolean isPalindrome(int x) {
        int num=0, temp=x,judge=0;
        while(x!=0){
        int i=x%10;
        x=x/10;
        num=num*10+i;
        }
        if(temp>=0)
        return (num==temp);
        else
        return (1==2);
    }
}